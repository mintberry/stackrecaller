-- open another file, bezierline doesn't show up: regen the overview together on some event? 
-- sometimes the overview doesn't show up
-- another more doc issue, the line height doesn't adjust properly

-- blank line to dismiss
-- brackets to dismiss
-- ovselect defect appear again?
-- sync bezier with focus area
-- select from ov

-- add a bezier
-- edit file,then rebuild tree
-- refine algorithm 
-- fix ov selected line display
------------
larger features:
-- multi-doc handle is a big issue
-- associate the tag with adornments
-- outlining encounters bug, need a workaround
-- edit handle
-- long files, ongoing
-- begin/end handle, scroll down, then focus area disapprear
-- display code when hover on ovline?